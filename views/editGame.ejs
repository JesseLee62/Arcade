<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit <%= game.name %></title>
  <link rel="stylesheet" href="/styles.css">
</head>

<body>
  <div class="container">
    <h1>Edit Game</h1>
    <form action="/games/<%= game._id %>?_method=PUT" method="post" class="edit-form">
      <div class="form-group">
        <label for="title">Title</label>
        <input type="text" id="title" name="title" value="<%= game.name %>" required>
      </div>

      <div class="form-group">
        <label for="description">Description</label>
        <textarea id="description" name="description" required><%= game.description %></textarea>
      </div>

      <div class="form-group">
        <label for="type">Type</label>
        <input type="text" id="type" name="type" value="<%= game.type %>" required>
      </div>

      <div class="form-group">
        <label for="minimumAge">Minimum Age</label>
        <input type="number" id="minimumAge" name="minimumAge" value="<%= game.minimumAge %>" required>
      </div>

      <div class="form-group">
        <label for="pricingHourly">Pricing (Hourly)</label>
        <input type="text" id="pricingHourly" name="pricingHourly" value="<%= game.pricing.hourly %>" required>
      </div>

      <div class="form-group">
        <label for="pricingPerGame">Pricing (Per Game)</label>
        <input type="text" id="pricingPerGame" name="pricingPerGame" value="<%= game.pricing.perGame %>" required>
      </div>

      <div class="form-group">
        <label for="imagePath">Image Path</label>
        <input type="text" id="imagePath" name="imagePath" value="<%= game.image.path %>" required>
      </div>

      <div class="form-group">
        <label for="imageAlt">Image Alt text</label>
        <input type="text" id="imageAlt" name="imageAlt" value="<%= game.image.description %>" required>
      </div>

      <button type="submit" class="btn save-btn">Save</button>
      <a href="/games" class="btn cancel-btn">Cancel</a>
    </form>
  </div>
</body>

</html>